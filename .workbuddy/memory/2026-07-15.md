# 2026-07-15 — 历史债修复（续上轮）

续上轮（2026-07-14）中断的会话，收尾剩余的 D6 + 同步文档。

## 上轮已完成的（2026-07-14 会话）
- D2 ✅ bash 程序+参数改造 (tryParseProgramArgs + bash-tool guard)
- D3 ✅ API Key 脱敏补全 (clipAndRedact 链式脱敏 + truncateForContext 摘要路径)
- D4 ✅ Quota 熔断 (QuotaGuard 注入 + streamWithFallback 跳过耗尽模型)
- D5 ✅ 事务边界注释 (connection.ts Plan B 注释 + grep 确认)

## 本轮完成的
### D6 ✅ Git 操作纪律
- `app/.husky/pre-commit`：tsc --noEmit + depcruise src
- `app/package.json`：添加 `"prepare": "husky"`
- `项目文档/git操作纪律-2026-07-15.md`：沉淀纪律规则

### D9 ✅ semantic_cache 索引优化
- `schema.ts`：加复合索引 (provider_name, task_type, model_id, expires_at)
- `semantic-cache.ts`：listValid() 支持选项过滤 + limit
- llm/semantic-cache.ts：lookupCache 传 embedding provider 名
- 新测 + 旧测共 18 条通过

### D1 ✅ Tauri 权限收窄
- `default.json`：fs 范围收窄到 $HOME/** + deny .ssh/.aws/.gnupg/.env*/secrets.*
- opener 收窄到 https://*；dialog:default；core 最小集

### D12 ✅ CSP 启用
- `tauri.conf.json`：替换 `null` 为 Tauri 兼容 CSP 字符串

### D11 ✅ ESLint
- `eslint.config.js` flat config
- `pnpm lint` exit 0（708 条 warn，exit 0）

### 修复的类型错误
- quota-guard.test.ts：routingDecision/finishReason 不存在于 UsageEventRow
- ChatWorkPanel.tsx + derive-workflow-diagnostics.ts：SwitchReason 新增 {kind:"quota"} 后 .category 访问不安全
- tsc 最终 clean exit 0

## 审查复盘发现
- 全量逐项审查 D1-D6/D9/D11/D12 完成（tsc / 2349 tests / depcruise / lint 全部通过）
- 唯一发现（低优先级）：D1 的 `core:default` 按计划应拆为 `core:event:default` + `core:window:default`，但实际用的是 `core:default`。这不是安全问题（Tauri 2 中 `core:default` 是合理最小集），可后续加固

## 验证结果
- 全量测试：206 files / 2349 tests ✅
- tsc --noEmit：clean ✅
- depcruise：no violations ✅
- ESLint：exit 0 ✅
- tauri build：编译 OK（仅 DMG 打包失败 — macOS 已知问题）

## 文档同步确认
- `剩余问题汇总.md`：§4.8 全部 D 项标记 ✅ 已修；§4.9 独立复查记录已写入；底部旧 W1-W10 排期段落已替换为完成状态说明；最后核对日期更新为 2026-07-15
- `历史债修复结果报告-2026-07-15.md`：完整结果报告已存在
- `git操作纪律-2026-07-15.md`：D6 纪律文档已存在
- `Cosmgrid-Agent-关键技术文档.md`：确认无需更新（架构文档，不含 D 项待办条目）

## Cloud 全面复查（下午）
用户让 Cloud 做了"剩下的事情"——实际 Cloud 做了大量新工作：
- 新增真 Skill 系统：`claude-code-compat/skill-loader.ts`（扫描 .claude/skills/*/SKILL.md）、`tools/skill-tool.ts`（模型调用入口）、`skills/capabilities.ts`（安全黑名单 K12）
- 工作流阶段重构：3 个伪 skill 退休为 workflow 原生注入（`phase-capabilities.ts`、`phase-guidance.ts`）；K7 门控从 skill-based 改为 phase-based
- 验收标准结构化：`evidence/verify-acceptance-criteria.ts` 从 registry 迁出
- Bug 修复：enforceCapabilities 方向反了、reducer repair 循环丢证据 ID、selector trigger 关键词过宽、seed insert-only 改 upsert
- 验证全通过：tsc clean / 206 files 2350 tests / depcruise no violations / eslint exit 0 / cargo check clean
- 57 files changed, 2675 insertions, 296 deletions（全部 uncommitted）
- E2/E3 仍未做（非安全，已记录）
- 文档未同步 Cloud 的新架构变更（待补）

## 文档同步完成（下午续）
已完成两份文档的同步更新：
- `Cosmgrid-Agent-关键技术文档.md`：
  - 日期头部加 2026-07-15 更新标注
  - §0.3 L6 加 skill-tool、L7 重写为架构重构描述（伪 skill 退休 + 真 Skill 系统 + K7 改造）、L9 加 phase-capabilities/phase-guidance
  - §0.5 目录归属加 skill-loader/skill-tool/phase-capabilities/phase-guidance + evidence 补 verify-acceptance-criteria
  - §3.1 工具清单加 skill-tool
  - §6.3 workflow 状态机加阶段行为纪律与能力门控段落
- `剩余问题汇总.md`：
  - §3 已完成大块表加"Skill 系统架构重构"行
  - 最后核对日期更新为"历史债 12 项全部修复完成 + Skill 系统架构重构文档同步完成"

## 桌面文档归档
3 份已完成方案文档从桌面归档到 `项目文档/归档文件/`，桌面原件移入废纸篓：
- `Cosmgrid-Agent-历史债修复交接方案-2026-07-15.md`（历史债修复交接方案，已执行完毕）
- `Cosmgrid-Agent-引擎化改造方案-2026-07-12.md`（引擎化改造方案，已落地）
- `jcode学习与历史债方案-2026-07-14.md`（历史债 v2 方案 + 学习计划，已执行完毕）

## 全量文档审计（下午）
逐份检查 `项目文档/` 8 份文档，核对代码后发现 5 项"文档说没做但代码已做"：
1. **Debate 裁决结构化** — 文档说"纯文本待办"，实际 `debate-engine.ts` 已有 `JudgeDecision {approved, feedback, finalSolution}` → 修了剩余问题汇总 §4.7、关键技术文档 §7.3/§12、完整方案 §0.5
2. **证据回放 UI** — 文档说"阶段4以后做"，实际 `EvidencePanel.tsx` 已存在且功能完整 → 修了剩余问题汇总 §4.7、索引文档
3. **acceptanceCriteria 结构化** — 文档担心"可能是 string[]"，实际 `StructuredAcceptanceCriterion {id, description, kind}` 已定义 → 修了剩余问题汇总 §5.2
4. **evidence-complete 用真实 messageId** — 文档担心"可能用 conversationId"，实际用精确 messageId 匹配 → 修了剩余问题汇总 §5.2
5. **task_outcomes 接入主链路** — 文档问"是否只在 eval runner"，实际 `stream-finalization.ts` 主聊天回调已调用 → 修了剩余问题汇总 §5.2
6. **§5.2 其余 5 项确认代码侧正确** — Model Profile 只生成建议、SmartRouter 区分 task/transport success、Candidate 人工批准 → 全部标 ✅
7. **Agent Job UI** — 真❌没做（后端有，前端无）→ 新增到 §4.7
8. **`.claude` frontmatter YAML** — 真❌没做（仍只支持扁平 key/value）→ 文档已标"已知非阻塞"
9. **应用打开方式.md** — branches 门槛写错（80%→75%）、缺 `pnpm lint` 命令 → 已修
10. **索引文档** — "之后按需做"列了 Debate 裁决增强和证据回放 UI（已做完）→ 已移除

## 归档文件全量审计（2026-07-15 续）

用户要求"已经归档的也要查"，对 `项目文档/归档文件/` 下 48+ 份 markdown 逐份检查，核对实际代码：

### 已归档但代码已全部落地的方案（归档正确，无需重做）
- **引擎化改造方案-2026-07-12.md** — PolicyStore 全部实现（`policy/` 16 文件 + 8 策略域 + 设置页 UI），但 §3 "已完成大块"表里漏了这条 → 已补录
- **MCP客户端与LSP桥接实施计划-2026-07-05.md** §八 P0/P1 必须修复项 — 全部落地（session-scope/secret-store/remote-client/client cleanup/lsp didChange）
- **规划方案-v2最优.md** v0.7/v0.8/v0.9 三阶段 — 全部实现
- **历史债修复交接方案-2026-07-15.md** D1-D12 — 全部修复（§4.8/4.9 已记录）
- **坑.md** 21 项 — 大部分 ✅，剩余 V0/V1/V3 真机验收 + 1.1 fallback + 3.2 remote embedding 仍需真机
- **Harness工程实施计划-2026-07-09.md** 阶段 0-5 — 有交付记录，已完成
- **未完成项清单-2026-07-03.md** 15 项 — 全部已在当前剩余问题汇总中追踪
- **当前状态与后续路线.md** — 已被剩余问题汇总取代，归档正确
- **下一步优先级方案.md** P0-P3 — 全部完成

### 文档同步缺口（已修）
- 引擎化改造（PolicyStore）未列入 §3 "已完成大块" → 已补录

### 仍然真正未完成的（已在剩余问题汇总中追踪，无需新增）
- S1-S9 真机验收（§4.2）
- Harness 真实任务闭环验证（§4.3）
- Eval 真实样本基线（§4.4）
- 远程 embedding/fallback 端到端（§4.5）
- Agent Job UI（§4.7）
- `code_task_v1` runtime（长期项，不阻塞）

